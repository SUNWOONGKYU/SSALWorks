# 에러 핸들링 패턴

## 요약
try-catch로 감싸고 개발자용 로그(console.error)와 사용자용 메시지 분리. 민감정보는 에러에 포함하지 않기. Supabase 호출 후 error 체크 필수.

## 상세

### 기본 패턴

```javascript
async function login(email, password) {
  try {
    const { data, error } = await supabase.auth.signInWithPassword({
      email, password
    });
    if (error) throw error;
    return { success: true, data };
  } catch (error) {
    console.error('로그인 실패:', error);  // 개발자용
    return {
      success: false,
      message: '로그인에 실패했습니다.'  // 사용자용
    };
  }
}
```

### Supabase 에러 코드 처리

```javascript
switch (error.message) {
  case 'Invalid login credentials':
    return { error: '이메일 또는 비밀번호가 틀립니다.' };
  case 'Email not confirmed':
    return { error: '이메일 인증이 필요합니다.' };
  default:
    return { error: '오류가 발생했습니다.' };
}
```

### 민감정보 보호

```javascript
// X: console.error(`비밀번호: ${password}`);
// O: console.error('로그인 실패:', { email, errorCode });
```

### HTTP 상태 코드

| 코드 | 의미 |
|------|------|
| 400 | 잘못된 요청 |
| 401 | 인증 필요 |
| 403 | 권한 없음 |
| 404 | 찾을 수 없음 |
| 500 | 서버 오류 |

---
📚 더 자세히: `브라우저_콘솔_에러_확인하기.md`
