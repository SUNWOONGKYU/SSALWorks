# 로컬 서버 실행 방법

## 요약
HTML 테스트 시 로컬 서버 필요. `npx serve` (권장), `python -m http.server`, VS Code Live Server 중 선택. 포트 충돌 시 다른 번호 지정.

## 상세

### 왜 서버가 필요한가?

| 기능 | file:// | http:// |
|------|---------|---------|
| CORS | X | O |
| ES 모듈 import | X | O |
| fetch API | X | O |
| Supabase SDK | X | O |

### 실행 방법 비교

| 방법 | 설치 | 자동 새로고침 |
|------|------|-------------|
| `npx serve` | 불필요 | X |
| `python -m http.server` | 불필요 | X |
| VS Code Live Server | 확장 설치 | O |

### 기본 명령어

```bash
# npx serve (권장)
npx serve Production/Frontend -l 3000

# Python
python -m http.server 8080
```

### 포트 충돌 해결

```bash
# 다른 포트 사용
npx serve -l 8080

# Windows: 사용 중인 프로세스 확인
netstat -ano | findstr :3000
taskkill /PID [PID번호] /F
```

---
📚 더 자세히: `브라우저_콘솔_에러_확인하기.md`
