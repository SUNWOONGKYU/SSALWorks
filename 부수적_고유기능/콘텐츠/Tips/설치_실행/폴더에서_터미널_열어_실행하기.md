# 폴더에서 터미널 열어 실행하기

## 요약
프로젝트 폴더에서 마우스 우클릭 → "터미널에서 열기" → `claude` 입력. Windows Terminal 설치 권장. 반드시 프로젝트 루트에서 실행해야 `.claude/CLAUDE.md` 설정 적용됨.

## 상세

### 가장 편리한 방법 (권장)

1. 파일 탐색기에서 프로젝트 폴더 열기
2. 빈 공간에서 **마우스 우클릭**
3. **"터미널에서 열기"** 클릭
4. `claude` 입력 후 Enter

### 다른 방법들

| 방법 | 설명 |
|------|------|
| 주소창에 `cmd` | 파일 탐색기 주소창에서 `cmd` 입력 |
| VS Code 터미널 | `Ctrl + `` 으로 터미널 열기 |
| cd 명령어 | `cd C:\Projects\my-project && claude` |

### 우클릭 메뉴 없을 때

Microsoft Store에서 "Windows Terminal" 설치 후 재부팅.

### 실행 위치가 중요한 이유

```bash
✅ C:\Projects\my-project> claude
   # .claude/CLAUDE.md 설정 적용됨

❌ C:\Users\username> claude
   # 설정 미적용
```

---
📚 더 자세히: Windows Terminal 공식 문서
