# 서브에이전트 효과적으로 활용하기

## 요약
복잡한 작업은 전문 서브에이전트에게 위임. `backend-developer`, `frontend-developer` 등 Task Agent가 작업, `code-reviewer` 등 Verification Agent가 검증. 작업자와 검증자는 반드시 분리.

## 상세

### 주요 서브에이전트

| Task Agent | 역할 | Verification Agent |
|------------|------|-------------------|
| `frontend-developer` | UI/UX 개발 | `code-reviewer` |
| `backend-developer` | API 개발 | `code-reviewer` |
| `database-specialist` | DB 설계 | `qa-specialist` |
| `security-specialist` | 보안 설정 | `security-auditor` |

### 작업자-검증자 분리

```
❌ 잘못된 예:
backend-developer가 작성 → backend-developer가 검증

✅ 올바른 예:
backend-developer가 작성 → code-reviewer가 검증
```

### 요청 방법

```
"backend-developer로 로그인 API 만들고
code-reviewer로 검증해줘"

"다음 순서로 작업해줘:
1. database-specialist: 테이블 설계
2. backend-developer: API 구현
3. test-engineer: 테스트"
```

### 주의사항
- 간단한 작업에 과도한 분업 피하기
- 복잡한 작업은 검증 단계 필수
- 작업 유형에 맞는 에이전트 선택

---
📚 더 자세히: `CLAUDE.md` 절대 규칙 2 (서브에이전트 검증)
