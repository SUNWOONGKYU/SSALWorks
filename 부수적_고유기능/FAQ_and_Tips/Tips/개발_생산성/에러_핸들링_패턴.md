# 에러 핸들링 패턴

## 💡 Tip

에러를 적절히 처리하면 디버깅이 쉬워지고 사용자 경험이 좋아집니다.

## JavaScript 에러 핸들링 패턴

### 1. try-catch (동기/비동기)

```javascript
try {
    const result = await fetchData();
    processData(result);
} catch (error) {
    console.error('데이터 처리 실패:', error);
    showUserError('데이터를 불러오는데 실패했습니다.');
}
```

### 2. 전역 에러 핸들러

```javascript
// 런타임 에러 캐치
window.onerror = function(message, source, lineno, colno, error) {
    console.error('전역 에러:', { message, source, lineno });
    // 에러 로깅 서비스에 전송
};

// Promise 거부 캐치
window.addEventListener('unhandledrejection', function(event) {
    console.error('Promise 거부:', event.reason);
});
```

### 3. API 응답 처리

```javascript
async function apiCall() {
    const response = await fetch('/api/data');

    if (!response.ok) {
        // HTTP 상태 코드별 처리
        switch (response.status) {
            case 401:
                showError('로그인이 필요합니다.');
                redirectToLogin();
                break;
            case 403:
                showError('권한이 없습니다.');
                break;
            case 404:
                showError('데이터를 찾을 수 없습니다.');
                break;
            case 500:
                showError('서버 오류가 발생했습니다.');
                break;
            default:
                showError('알 수 없는 오류가 발생했습니다.');
        }
        return null;
    }

    return response.json();
}
```

## Supabase 에러 처리

```javascript
const { data, error } = await supabaseClient
    .from('users')
    .select('*');

if (error) {
    console.error('Supabase 오류:', error.message);
    // 사용자에게 친절한 메시지 표시
    showError('데이터를 불러오는데 실패했습니다.');
    return;
}

// 성공 시 처리
processData(data);
```

## 사용자 친화적 메시지

```javascript
// ❌ 기술적 메시지
alert('Error: ECONNREFUSED');

// ✅ 친절한 메시지
alert('서버에 연결할 수 없습니다. 잠시 후 다시 시도해주세요.');
```

## 관련 파일
- `S2_개발-1차/Backend_Infra/S2BI2_global_error_handler.js`
