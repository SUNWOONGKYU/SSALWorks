# 환경변수 설정 순서

## 질문
환경변수를 설정할 때 어떤 것부터 해야 하나요?

## 답변

### 필수 환경변수 우선순위

**1순위: Supabase (필수)**
```env
SUPABASE_URL=https://[프로젝트ID].supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIs...  # 서버 전용
```

**2순위: Google OAuth (로그인 필요 시)**
```env
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxx
```

**3순위: 토스 페이먼트 (결제 필요 시)**
```env
TOSS_CLIENT_KEY=test_ck_xxx
TOSS_SECRET_KEY=test_sk_xxx
```

**4순위: Resend (이메일 발송 필요 시)**
```env
RESEND_API_KEY=re_xxx
```

**5순위: AI APIs (선택)**
```env
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx
```

### 클라이언트 vs 서버 구분

| 환경변수 | 위치 | 보안 |
|---------|------|------|
| SUPABASE_URL | 클라이언트 OK | 공개 가능 |
| SUPABASE_ANON_KEY | 클라이언트 OK | 공개 가능 |
| SERVICE_ROLE_KEY | 서버 전용 | 🔒 비공개 |
| SECRET_KEY | 서버 전용 | 🔒 비공개 |

### Vercel 환경별 설정

```
Development → .env.local 파일
Preview → Vercel Dashboard (PR/브랜치)
Production → Vercel Dashboard (프로덕션)
```

### ⚠️ 절대 금지
- 환경변수 파일 Git 커밋
- API 키 코드에 하드코딩
- SERVICE_ROLE_KEY 클라이언트 노출

### 관련 문서
- `S1_개발_준비/Backend_Infra/Environment/ENV_SETUP.md`
